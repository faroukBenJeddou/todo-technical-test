{
  "name": "mern-junior-tech-test-json",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "setup": "npm install -w client && npm install -w server",
    "dev": "npm-run-all -p dev:server dev:client",
    "dev:server": "npm -w server run dev",
    "dev:client": "npm -w client run dev",
    "test": "npm -w server test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}